#cloud-config

users:
  - name: ${USER}
    groups: users
    shell: /bin/bash
    sudo: ['ALL=(ALL) NOPASSWD:ALL']
    plain_text_password: ${PASSWORD:-letmein}
    ssh_authorized_keys:
    - ${SSH_PUBLIC_KEY}
