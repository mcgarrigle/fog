#cloud-config

users:
  - name: ${USER}
    groups: users
    chpasswd:
      expire: False
    shell: /bin/bash
    sudo: ['ALL=(ALL) NOPASSWD:ALL']
    passwd: ${PASSWORD:-XXXXXXXX}
    ssh_authorized_keys:
    - ${SSH_PUBLIC_KEY}
